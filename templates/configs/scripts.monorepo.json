{
  "description": "Monorepo root npm scripts for quality checks (delegates to workspaces)",
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "lint:check": "npm run lint:check --workspaces --if-present",
    "prettier": "npm run prettier --workspaces --if-present",
    "prettier:check": "npm run prettier:check --workspaces --if-present",
    "cq:local": "concurrently -n 'typecheck,lint,prettier' -c 'yellow,blue,green' 'npm run typecheck' 'npm run lint' 'npm run prettier'"
  }
}
